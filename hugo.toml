baseURL = "https://bullish-intelligence.github.io/"
title = "Bullish Intelligence"
languageCode = "en-us"

# Optional but harmless with modules; Docsy docs often omit this if using [module] imports.
theme = ["github.com/google/docsy"]

[module]
  proxy = "direct"

  [module.hugoVersion]
    extended = true
    min = "0.120.0"

  [[module.imports]]
    path = "github.com/google/docsy"

  [[module.imports]]
    path = "github.com/google/docsy/dependencies"

